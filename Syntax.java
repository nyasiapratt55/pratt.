import java.util.Scanner;

/**
   ) Write a program or an extension to the above program that
determines if the tokens conform to the correct syntax
*/
public class Syntax
{
   public static void main(String[] args)
   {
      Scanner in = new Scanner(System.in);

      // Get the names of the couple

      System.out.print("correct syntax token: ");
      String first = in.next();
      System.out.print("if not print error sign ");
      String second = in.next();

      // Compute and display the inscription

      String token first.substring(0, 1) 
         + "&" + second.substring(0, 1);
      System.out.println(syntax );
   }
}
